# Public site URL — used for metadataBase (OG image absolute URLs).
# Vercel sets VERCEL_URL automatically for preview deploys, so this is
# only needed for production (set in Vercel project settings).
NEXT_PUBLIC_SITE_URL=https://your-domain.dev
